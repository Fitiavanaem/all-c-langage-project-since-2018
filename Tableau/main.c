#include <stdio.h>
#include <stdlib.h>
#define N 100

int main()
{int T[N],i;

  for(i=0;i<N;i++)
      T[i]=3;
  for(i=0;i<N;i++)
  {
    printf("l'element du tableau sont:%d\n",T[i]);
  }
    return 0;
}
